paramkit modules API
=====================

These modules are WASM sub-parsers used by the VS Code extension for specialized parsing tasks (color values, texture strings, etc.). Some modules, like `core`, are referenced and loaded dynamically.

.. doxygenindex::
   :project: paramkit_modules
