# Exclude everything that isn't needed in the packaged VSIX.
# The extension is bundled into out/extension.js by `bun build`,
# so node_modules are not required at runtime.

.vscode/**
.vscode-test/**
src/**
tsconfig.json
bun.lockb
bun.lock
node_modules/**
*.vsix
